PORT=3000
NODE_ENV=development

# Database Configuration (for Prisma)
DATABASE_URL="postgresql://postgres:password@localhost:5432/skytrack?schema=public"

# Legacy Database Configuration (can be kept for backwards compatibility)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=skytrack
DB_USER=postgres
DB_PASSWORD=password
DB_MAX_CONNECTIONS=10
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000